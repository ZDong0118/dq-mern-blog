import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    marginTop: '2rem',
    display: 'flex',
    alignItems: 'center',
  },
  notes: {
    marginBottom: '2rem',
  },
}));
