import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  container: {
    fontFamily: 'Arial',
    minWidth: '320px',
  },
  mainTitle: {
    fontSize: '24px',
  },
}));
